[providers]
node = "18"

[phases.build]
cmds = ["npm ci --production=false", "npm run build"]

[phases.start]
cmd = "npm run start"

[variables]
NODE_ENV = "production"
NPM_CONFIG_PRODUCTION = "false"